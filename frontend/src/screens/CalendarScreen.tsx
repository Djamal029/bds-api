/**
 * STUB — not implemented. Same data source as MatchesScreen.tsx
 * (`getUpcomingFixtures()`), grouped by date instead of a flat list.
 * A screen doesn't need its own API client function just because its
 * layout differs — reshaping already-fetched data for display is a
 * frontend-only concern, not a reason to add a backend route.
 */

export {};
