/**
 * STUB — not implemented. Full list from `getUpcomingActivities()`
 * (api/activities.ts), each card showing `spots_remaining`/
 * `is_registered` and a register/unregister button calling
 * `registerForActivity(...)`. Copy screens/LoginScreen.tsx for the
 * loading/error-state pattern, and its error-catching shape for
 * surfacing `ActivityFullError` (see api/v1/activities.py's stub) as a
 * readable message instead of a generic failure.
 */

export {};
