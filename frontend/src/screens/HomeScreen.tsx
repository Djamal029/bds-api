/**
 * STUB — not implemented. Copy screens/LoginScreen.tsx's `useState` +
 * async-call pattern, loading from `getUpcomingFixtures()` (api/
 * fixtures.ts) and `getUpcomingActivities()` (api/activities.ts) in
 * parallel via `Promise.all([...])` on mount, rendering both as
 * horizontally-scrolling previews with a "see all" link to the full
 * MatchesScreen/ActivitiesScreen.
 */

export {};
