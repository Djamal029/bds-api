/**
 * STUB — not implemented. Live score entry for one fixture: two
 * increment buttons (home/away) calling a debounced or on-confirm
 * `updateFixtureScore(...)` you'd add to api/fixtures.ts, matching
 * services/fixture_service.py's stub. Takes the fixture as a navigation
 * param rather than re-fetching it by id, since the previous screen
 * (ReporterScreen.tsx) already has the full object in hand.
 */

export {};
