/**
 * STUB — not implemented. A list of fixtures the current reporter can
 * start/score, each row navigating into ReporterFixtureScreen.tsx.
 * Reserved for a "score reporter" permission flag that doesn't exist in
 * this skeleton's two-role (member/administrator) model — see
 * api/deps.py's `require_role` for how to add a third role or a
 * boolean flag once needed.
 */

export {};
