/**
 * STUB — not implemented. Copy screens/LoginScreen.tsx almost exactly:
 * same form shape, calls `register(email, password)` from api/auth.ts
 * instead of `login(...)`, then typically chains into `login(...)`
 * right after so a fresh account is signed in immediately rather than
 * bounced back to a login screen it just came from.
 */

export {};
