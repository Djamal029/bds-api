/**
 * STUB — not implemented. Loads `getMyPayments()` (api/payments.ts),
 * shows each payment's status, and a "request a code" action calling
 * `requestPayment(...)` when nothing is pending yet.
 */

export {};
