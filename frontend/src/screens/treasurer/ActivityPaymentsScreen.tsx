/**
 * STUB — not implemented. Loads `getActivityPaymentDues()` (api/
 * activity-payments.ts, also a stub), grouped into "pending" and
 * "already reviewed", each pending row with validate/reject actions.
 * This is the one payments screen in the real project that was always
 * wired up to real data from the start — use it (once implemented) as
 * the reference for admin/AdminPaymentsScreen.tsx's stub, which
 * explicitly warns against the alternative (a screen that looks real
 * but renders fake data).
 */

export {};
