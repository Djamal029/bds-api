/**
 * STUB — not implemented. Purely local state (which fixtures/activities
 * a user starred), not backed by any API route in this skeleton — a
 * reasonable first implementation is `AsyncStorage` (from
 * `@react-native-async-storage/async-storage`) holding a list of ids,
 * with no backend involvement at all. Only add a backend table for this
 * if favorites need to sync across a user's devices.
 */

export {};
