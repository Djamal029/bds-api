/**
 * STUB — not implemented, and needs a new backend route first:
 * `GET /activities/mine` (see api/v1/activities.py's stub — add a
 * `list_mine(user_id)` method to ActivityService returning only
 * activities the caller is registered for) plus a matching
 * `getMyActivities()` in api/activities.ts. This screen is then a plain
 * list render, same shape as ActivitiesScreen.tsx.
 */

export {};
