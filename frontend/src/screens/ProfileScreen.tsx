/**
 * STUB — not implemented. Shows `getMe()` (api/auth.ts)'s result, a log
 * out button (see api/auth.ts's `logout()`), and navigation rows into
 * EditProfileScreen/MyPaymentsScreen/MyRegistrationsScreen, plus a
 * conditional row into the admin area only when `user.role ===
 * 'administrator'` — see CONTRIBUTING.md's note that a hidden button is
 * not a security control, the real gate is still the backend's
 * `require_admin` dependency, this is only about not confusing a
 * regular member with a button that would just 403 anyway.
 */

export {};
