/**
 * STUB — not implemented. Loads `getNotifications()` (api/notifications.ts),
 * tapping a row calls `markNotificationRead(id)` then, if the
 * notification has a `link`, navigates there — the exact navigation
 * mechanism depends on whichever navigator you set up (see
 * navigation/, currently empty, and frontend/README.md).
 */

export {};
