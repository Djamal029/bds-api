/**
 * STUB — not implemented. A menu of links into the other admin/*
 * screens (Users, Activities, Players, Payments, Import) — no data
 * fetching of its own, just navigation. Only reachable from
 * ProfileScreen.tsx's conditional admin row (see that screen's stub).
 */

export {};
