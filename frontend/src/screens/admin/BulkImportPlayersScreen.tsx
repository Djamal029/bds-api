/**
 * STUB — not implemented, and well out of scope for a first
 * contribution: uploading a spreadsheet of (email, team) rows to
 * bulk-recruit players in one pass. Needs `expo-document-picker` (not
 * installed in this skeleton) on the frontend and a real multipart
 * file-upload backend route before this screen has anything to call —
 * build and test the backend half first, in isolation, before touching
 * this file.
 */

export {};
