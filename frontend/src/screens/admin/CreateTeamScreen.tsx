/**
 * STUB — not implemented. A form (name, sport picker, season) submitting
 * to a `createTeam(...)` you'd add to api/teams.ts, matching a
 * `POST /teams` admin-only route you'd add to api/v1/teams.py. Copy
 * screens/LoginScreen.tsx's form-handling pattern.
 */

export {};
