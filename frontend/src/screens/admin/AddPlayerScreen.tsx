/**
 * STUB — not implemented. A form (name, team picker) submitting to a
 * `recruitPlayer(...)` you'd add to api/recruitment.ts, matching
 * services/recruitment_service.py's stub — chains straight into
 * generating that player's registration code on success, so the admin
 * gets a code to hand over in one flow rather than two separate screens.
 */

export {};
