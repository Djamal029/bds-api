/**
 * STUB — not implemented. Browse teams/rosters, with a link into
 * TeamDetailScreen.tsx per team, plus admin-only actions (add a player,
 * generate a registration code via `generateCode(playerId)` you'd add
 * to api/recruitment.ts) once those backend routes exist.
 */

export {};
