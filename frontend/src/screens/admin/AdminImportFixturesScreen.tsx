/**
 * STUB — not implemented, and well out of scope for a first
 * contribution: bulk-importing historical fixture results from an
 * uploaded file. See `expo-document-picker` for the file-picker
 * mechanism (not installed in this skeleton) and
 * models/import_history.py's stub for the backend side. Needs a real
 * backend endpoint accepting a file upload before this screen has
 * anything to call — build the backend half first.
 */

export {};
