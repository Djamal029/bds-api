/**
 * STUB — not implemented. Loads every account via a `getUsers()` you'd
 * add to a new api/administration.ts (matching `GET /administration/users`,
 * see api/v1/administration.py's stub), with per-row actions
 * (activate/deactivate, change role) — each action's backend route
 * already enforces "an administrator can't touch another administrator's
 * account" server-side (see that route file's stub), so this screen
 * doesn't need to re-implement that check, only handle the 403 it might
 * get back gracefully.
 */

export {};
