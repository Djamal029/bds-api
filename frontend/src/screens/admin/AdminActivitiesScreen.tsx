/**
 * STUB — not implemented. Create/edit/delete activities — a list from
 * `getUpcomingActivities()` (api/activities.ts) with admin-only actions
 * calling routes you'd add once `api/v1/activities.py`'s stub is
 * implemented (`PUT`/`DELETE /activities/{id}`, not shown in that
 * file's stub sketch but the same shape as its `create`).
 */

export {};
