/**
 * STUB — not implemented. Loads every payment via a `getAllPayments()`
 * you'd add to api/payments.ts (matching a `GET /payments` admin/
 * treasurer-only route you'd add to api/v1/payments.py), with
 * validate/reject actions per row — same shape as
 * treasurer/ActivityPaymentsScreen.tsx (also a stub) for player
 * jersey/license payments instead of activity installments.
 *
 * WARNING for whoever builds this: do not ship a version that renders
 * hardcoded sample data with a "not wired up yet" banner instead of a
 * real API call — that shipped in the real project this skeleton is
 * modeled on for a while, and it actively misled an administrator who
 * trusted what looked like real payment records. If the backend route
 * isn't ready yet, leave this screen unreachable (no link to it) rather
 * than reachable-but-fake.
 */

export {};
