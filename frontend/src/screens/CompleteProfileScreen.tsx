/**
 * STUB — not implemented. Copy screens/LoginScreen.tsx's form-handling
 * pattern. Shown instead of the main app when a logged-in user has no
 * `username` yet (see backend/.../schemas/auth.py's `UserRead.username`,
 * nullable) — a username-only form calling `updateProfile({ username })`
 * from api/auth.ts. Which screen a logged-in user sees (this one vs. the
 * main app) is a navigation-level decision, not this screen's own
 * concern — see navigation/ (currently empty, see frontend/README.md).
 */

export {};
