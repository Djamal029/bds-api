/**
 * STUB — not implemented. Takes a `teamId` navigation param, loads that
 * team's roster (a `getTeamPlayers(teamId)` you'd add to api/teams.ts,
 * matching a `GET /teams/{team_id}/players` route you'd add to
 * api/v1/teams.py), and renders it as a plain list.
 */

export {};
