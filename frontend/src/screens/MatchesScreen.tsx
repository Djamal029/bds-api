/**
 * STUB — not implemented. Full list of fixtures from `getUpcomingFixtures()`
 * (api/fixtures.ts, also a stub), rendered with `FlatList`. Copy
 * screens/LoginScreen.tsx for the loading/error-state pattern.
 */

export {};
