/**
 * STUB — not implemented. A form pre-filled from `getMe()`, submitting
 * via `updateProfile(...)` (api/auth.ts). Copy screens/LoginScreen.tsx's
 * form-handling pattern exactly — the only real difference is loading
 * initial values on mount instead of starting from empty fields.
 */

export {};
